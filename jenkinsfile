pipeline {
    agent any
   
    stages {
        stage('build') {
            steps {
                echo 'inside build method'
            }
        }
        
        stage('test') {
            steps {
                echo 'inside testing phase'
            }
        }
        
        stage('deploy') {
            steps {
                echo 'inside deploy phase'
            }
        }
       
    }
}
